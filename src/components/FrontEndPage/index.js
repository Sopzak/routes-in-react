import React from 'react';
import BackButton from '../BackButton';

const FrontEndPage = () => (
  <div>
    <h1>React.js</h1>

    <p>
        React is a Javascript library to create user interfaces and, in this context, one of the tecnologs more uses by the companys.
    </p>

    <BackButton />
  </div>
);

export default FrontEndPage;