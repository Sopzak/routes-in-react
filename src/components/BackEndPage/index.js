import React from 'react';
import BackButton from '../BackButton';

const BackEndPage = () => (
  <div>
    <h1>Node.js</h1>

    <p>
        Node.js is a framework used to rum JavaScript out of browser. With he we can make applications like websites or microservices.
    </p>

    <BackButton />
  </div>
);

export default BackEndPage;